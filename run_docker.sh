#!/bin/bash

# Script to build and run the BPMN Generator with Flask UI using Docker

echo "Building and starting BPMN Generator with Flask UI..."

# Build and start the services
docker-compose up --build